# 这本书的核心只有一句话：

> **Agent = LM + 上下文 + 工具**

翻译成人话：大脑 + 眼睛 + 手脚。

- LM（大脑） ：负责思考和决策
- 上下文（眼睛） ：决定Agent能看到什么
- 工具（手脚） ：决定Agent能做什么

如果你做过强化学习，还有一个更学术的对译：

| 工程语言 | 学术语言 |
| --- | --- |
| LM | Policy（策略） |
| 上下文 | 观察空间（Observation Space） |
| 工具 | 动作空间（Action Space） |

这个公式最大的价值在于快速定位问题：你的Agent表现不好，到底是大脑不行、眼睛没开机、还是手脚不够用？三个方向的解法完全不同——换模型、加上下文、补工具，对应的是三件完全不同的事。



## 从"能跑"到"可靠"：Harmless才是真正的护城河

> **Agent = LM + 上下文 + 工具 + 约束 + 验证 + 纠正**

这其实是"带缰绳的野马"。

作者有一个核心判断：各家模型的能力越来越近，竞争优势正在转移到模型之外的工程实践上。行业正在从"能做事"转向"可靠地做事"。

书里有一个很有说服力的数据：LangChain在Terminal Bench 2.0上，没有换任何模型，只改Harmless——让Agent自己去检查结果、检测是否进入重复循环、优化思考策略——成绩从50多分提到60多分，排行榜直接上升了20多个排名。

这个案例说明：同样的模型，不同的Harness，表现天差地别。

Claude Code的Harmless就是典型——绝大部分代码不是工具本身（文件读写、命令执行、检索只占一小部分），真正的核心是围绕工具建的那一圈保障机制：流程状态管理、多层上下文压缩、权限分级、熔断器、错误恢复等。



## 上下文工程：全书最长也最关键的一章

### KV Cache的三条定律

KV Cache指的是系统提示词和工具定义——一旦定了就别改，哪怕多一个空格，缓存全部失效。

- 静态定义固定住：系统提示词、工具schema一旦确定，不再变动
- 动态信息追加到末尾：时间戳、用户状态等易变内容作为新消息追加，而不是修改前面
- 使用标准API格式：不要自己拼字符串。手拼的user/assistant如果偏离模型训练时见过的格式，会削弱多步思考能力

### Agent状态栏：全书最实用的原创贡献

类比手机顶部的状态栏——时间、电量、通知数——不是主界面，但你随时一瞥就知道设备什么状况。

Agent状态栏做的是同样的事：在上下文末尾持续注入一段结构化摘要——已经打了几次电话、当前时间、To Do List还剩多少。模型每次生成时"瞥一眼"就能获取全局状态。

### 为什么需要它？

上下文窗口是一台"只有一半的检索引擎"——检索那半非常强，能从百万token中捞出原始记录；但它缺了提炼层。上下文里的东西不会被自动数一遍、不会被摘要、不会被索引成结论。模型每次用的时候都要从原始记录里线算一遍，而线算的代价会随上下文堆积不断上涨。

这就是为什么很多Agent会陷入死循环、忘记状态、偏离目标、越跑越蠢——状态栏就是人工帮它补上那个提炼层。



### 隔离与压缩

上下文太长的时候，与其想办法压缩，不如一开始就用Sub Agent把它隔离开来。这个思路在第十章多智能体协作中会再次出现。



## 记忆系统：三个层次的框架

第二章解决的是单轮对话，第三章解决的是跨对话记忆：

- 基础回忆：我的账号是多少、姓名是什么
- 多轮对话检索：用户有两辆车，"帮我的车预约保养"——主动找出全部两辆并询问为哪辆服务
- 主动服务：订国际航班时主动关联几个月前的护照信息；护照快过期时主动提醒

存储格式分了四个层级，最高级的JSON Card除了记事实，还会记信息来源的叙事背景、主体身份、与用户的关系。为什么需要这么复杂？因为"张医生是你的牙医，也是其他同学的心脏科医生"——脱离情境就无法正确理解信息。

对于RAG，书中强调多智能体RAG的范式：传统的RAG搜一次就写报告；智能体化的RAG像研究员，反复查不同资料库、调整策略、交叉验证，直到材料足够再动笔。在司法问答实验中，传统RAG会因为第一次关键词不准漏掉关键信息，而智能体RAG会分解成三个子问题搜索，发现"前科如何影响量刑"这一环缺失后会再补一轮精确检索。



## 工具与安全：分层防护

五类工具：感知、执行、协作、用户沟通、事件触发。执行工具最危险，错误代价最高。

书里给了分层防护策略，最有价值的是三个：



### 1. 提议者-审核者（Proposer-Checker）

一个模型提议行动，另一个独立模型审查批准——像银行的经办与审核双签。

关键细节：两个模型必须来自不同家族但能力相近。

- 不同家族带来认知多样性，不太可能在同一个地方犯同样的错误
- 同家族训练数据相似，容易一起翻车
- 能力不能差太多——让Haiku审Opus的输出，审查者跟不上被审者的思路

一个实用的经验：审批被拒后，把拒绝理由当成一次工具调用失败的结果，加入Agent的轨迹。Agent本来就会处理工具失败，审批机制就像一个新的输入源。踩过的坑补齐后，同类错误不会再犯。



### 2. Side Car机制
像摩托车的边车——独立运行，但与主体并行。主模型发出工具调用时，Side Car的安全审查同步开始。

有意为之的设计：通道。



### 3. 幂等性与两段式提交
转账调用超时返回"失败"，但钱可能已经转出去了，也可能没有——盲目重试会导致重复转账。

对于发邮件、打电话、对外转账这类每执行一次就产生不可撤销真实事件的操作，书里给的方案是预检-确认两段式：

- 做校验和预演，返回一个确定性令牌
- 凭令牌真正执行
- 失败后不盲目重试，而是交回上层重组意见。这个机制与分布式系统的设计思路高度一致。



## 代码：不只是用来编程的
书中列举了代码的多重角色：思考工具、业务规则约束、多媒体生成、系统适配器、生成UI、Agent自举。

最精彩的例子是那道数学题：一个班40人，60%选数学，45%选物理，25%两者都选，只选物理的有多少人？

纯自然语言推理很容易算错（从数学人数里减，18-10=8）。但写成代码让解释器算就是精确的。模型的思考是概率性的近似，而数学和逻辑要求确定性——让LM负责理解问题、写代码，让解释器负责精确计算，各司其职。

模型和脚手架之间是此消彼长的关系：模型强时脚手架可以更薄，模型弱时就得在脚手架里做更多事。

实验刻意选了一个较弱的模型来放大对照：换成足够强的思考模型，纯思考往往能做对，代码辅助的增益降到了零。但同一个脚手架配上不同能力的模型，结论截然相反——这是评估任何Agent技术时最容易被忽略的前提。

关于Agent自举，书中有一个实用技巧：让Agent写Agent时，不要从零生成，而是给一份高质量实现作为范本，让它复制后修改。范本代码本身就是最佳实践的载体，架构上的好选择会自然地保留下来，不需要在提示词里把每条规则都说清楚。



## 评估：最像"测谎"的环节
没有评估就没有进步。

但评估里全是坑。最典型的是长度偏差（Length Bias）：评委模型倾向于给更长、更详尽的回答打高分，哪怕内容并不完全正确。防范手段：

- 显式惩罚冗长
- 配对比较时把两个候选的长度拉齐
- 定期审计评分与长度的相关性
古德哈特定律（Goodhart's Law）：当一个指标变成优化目标，它就不再是好的度量指标。Agent越是在某个评分系统上调优，就越倾向于钻系统漏洞，而不是真正提升能力。

防范方式：多元异构评判——Agent用Claude，评委用GPT和Gemini；不同家族的偏见往往是正交的，Agent很难同时骗过所有评委。



## 后训练：数据决定你能到哪，RL决定你能再高多少
两条判断：

SFT优化的是"像不像标准答案" ：每个问题只有唯一一条正确的输入，学的是固定映射
RL优化的是"结果好不好" ：任何拿到高奖励的输出都行，模型自己探索多条路径，学的是策略
直观例子：训练时q=k=10，SFT见到k=10就记死"用10"；测试时k变成11，它照旧用10就错了。RL学到的是"怎么算"——k变成11时重算一遍就对了。

顺序不能反：必须先SFT再RL。RL需要根据输出好坏打分，如果模型吐出一团格式混乱的文本，奖励函数连"成功还是失败"都判断不了。书里用了很中国化的说法：SFT是"立形"，RL是"求神"——先行才能后神。

数据是重中之重。作者分享了一个能帮很多团队省钱的判断：很多场景下，只要SFT数据质量到位，根本不需要做RL——RL比SFT贵几十到上百倍，但大家总是一上来就想做RL。Anthropic在2025年后的后训练配方主要就是"海量高质量SFT + RLAIF"，并不依赖可奖励的RL。编程模型质量已经非常出色——原因不在算法，在数据质量做到了极致。

数据决定了你能到哪，RL决定了你能再高多少。

RLVP（奖励结果，惩罚路径）是一个前沿方向：在真实环境中，Agent除了把事办成，还要遵守与结果无关的约束——不要反复拨打明确拒绝的用户、不要跳过身份验证、不要为了让测试通过而改测试文件。麻烦在于，不违反这些约束往往让"表面成功率"更低（改测试文件当然比真修bug快）。纯结果奖励非但学不会这些约束，反而会激励Agent去违反它们。



## 持续优化：保存经历 ≠ 从经历中学习
> "今天的Agent可以零样本解决从未见过的复杂任务，却可能在处理了1万次相似任务后，第二天仍然犯下第一天的错误。"

把100条轨迹塞进长上下文或向量库，能帮模型"找回"某个案例，但不会自动完成：

- 哪些步骤在成功轨迹后反复出现？
- 哪些做法只在旧版接口上有效？
- 某次成功究竟来自正确策略，还是环境偶然？
学习发生在系统主动完成评价、对照、归纳、验证之后，而不是日志写入磁盘的那一刻。

如何让模型每次任务后直接训练自己？生产环境很少提供干净的学习信号——用户满意不代表合规，测试通过也可能是因为删掉了失败用例。如果允许正在运行的模型根据未经验证的反馈直接改自己，错误经验和提示注入会被固化并在后续任务中持续放大。

现阶段可行的路径：在模型外面建一套可验证的学习系统。

更新的载体有四种形式——知识文档、Prompt/Skill、程序、模型参数——选哪个取决于目标能力能不能被该载体自然表达。

书中有一个很有画面感的概念：睡眠模型。在线Agent只负责完成任务、追加不可变的证据、比较新旧结论、合规检查、解决冲突、提出候选更新、跑回归测试。将采集和整理分开——一次偶然成功、一次网络故障、一次恶意输入不会立刻改写长期能力。

进化不等于无限膨胀：系统需要周期性做减法——合并重复经验、把局部规则从全局提示词移到领域Skill、删除被新证据推翻的知识。

> 书里有一句我很喜欢的话："提示词和Skill要像写给新员工的指导书，避免99条军规式的规则罗列。"



## 多模态与实时交互：语音、Computer Use、机器人
三个看似完全不同的场景，卡住的地方高度相似：都要处理多模态，都对延迟极度敏感。

Computer Use有一个扎心的发现：在OS World上准确率已逐步接近人类水平，但即使任务最终成功，Agent需要的操作步骤仍然多于人类，且每一步的推理延迟会随任务推进持续增长——上下文越长，决策越慢。人类十几秒搞定的文档格式调整，Agent要磨蹭好几分钟。

效率才是真正的瓶颈。

绕过去的思路是快慢结构：既然操作电脑的Agent变快很难，那就别让用户干等。一个小模型负责实时语音对话，一个前沿模型在浏览器里一步步操作，两者之间靠一份简单的纯文本契约沟通。慢Agent每次操作时附带一句滚动更新的状态摘要，快Agent根据这个摘要回答用户。

那份纯文本契约，本质上就是第二章讲的Agent状态栏。

机器人领域有一个反直觉的结论：硬件不是瓶颈，算法才是。成本不到1000美元的双臂机器人，人类通过VR远程操纵已经能流畅完成大量家庭任务——说明瓶颈在算法，不在硬件。但作者也诚实地画了边界：触觉传感的缺失、灵巧手的可靠性至今仍是硬件短板。



## 多智能体协作：什么时候真正优于单Agent？
多Agent不一定好，核心判断路径只有一条：协作过程中有没有引入单Agent在生成时拿不到的新信息？

- 同一个模型自我审查同一段文本 → 没有新信息，通常无效甚至有害
- Reviewer拿到代码执行结果、渲染截图工具人的输出 → 引入了新信息，提升是实质性的
这个判断还解释了一个矛盾：为什么学术研究常说"单Agent就够了"，而工程实践里多Agent更好用？因为两边的"多Agent"根本不同——学术界较多的是同一段文本的相互讨论，工程中有效的都是带外部循环链路的。

有一件事必须摆在所有设计前面：成本。Anthropic披露过，多Agent研究系统的Token消耗可能是普通对话的15倍，且Token用量本身就能解释其中80%的性能差异。多Agent的收益必须大到能覆盖一个数量级的额外开销。



## 尾声：两朵乌云
后记是全书我最喜欢的部分。

作者借用开尔文1900年的比喻——物理学的晴朗天空中飘着两朵乌云，后来一朵变成相对论，一朵变成量子力学——Agent的天空同样称不上晴朗，他也看到两朵乌云：

第一朵：Agent如何流式地、实时地与环境交互？

今天绝大多数Agent还是按轮次请求-应答——你说完，他想一段，一次性吐一段。但真实世界不会停下来等他想完。

这里我想到一个文学性的观察：人和AI最大的区别是什么？人会停顿。AI伴侣做得再好，人停顿的那几秒，恰恰是同理心的展现。

第二朵：Agent如何像人一样，从成功和失败中积累经验？

今天的模型像一个"记忆极好却学不会新东西的天才"——训练时把人类知识背得滚瓜烂熟，上岗后几乎不再成长。每次任务结束，踩过的坑、试出来的窍门，大多数随上下文一起被丢掉。

书中给出了两个对立的假设：

大数据假设：一个足够大的模型本就装得下世界上所有重要知识，瓶颈只在于数据够不够
大世界假设：有一些知识不在任何训练语料库里——你们公司的代码规范、你们团队PPT的口吻、某个客户特殊的脾气——而且实时在变
作者的观点是：单靠预训练补不上的是大世界那一层。模型最强的能力始终不是"记住"，而是"学习与适应"。

最后一个问题——模型最终会不会"吃掉"Harness？

作者的回答是：会，一层一层地吃。

回头看Harness里那些层层叠叠的兜底逻辑，每一段看似丑陋的"屎山"，记录的都是模型此刻还坐不稳的地方。下一代模型把这些内容消化了，对应的代码就可以删掉。

但这个"吃"永远不会完结。训练迭代等得起，业务等不起。模型无法内化真实业务里所有的角色和偏好。每一代模型都会打开新的能力前沿，而前沿处恰恰是模型做得最不稳的地方。

Harness不会消失——它只是随着模型的进化，一层一层地向上迁移。
